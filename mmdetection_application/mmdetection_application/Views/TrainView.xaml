<UserControl x:Class="mmdetection_application.Views.TrainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:mmdetection_application.Views"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <DockPanel>
        <DockPanel DockPanel.Dock="Bottom"/>
        <DockPanel DockPanel.Dock="Top">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Margin="10">
                <TextBlock Text="Train" FontSize="100" Foreground="White"/>
            </StackPanel>
        </DockPanel>
        <Grid>
            <TextBlock Text="Choose Model" FontSize="30" Foreground="White"/>
        </Grid>
    </DockPanel>
</UserControl>
